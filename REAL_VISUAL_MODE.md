# Real visual mode

AutoPoster video generation uses Wan2.1 text-to-video clips as the visual source. The renderer must not use static-image, text-card, typography, presentation-slide, or animated-circle fallbacks.

Each scene should be concrete, physically observable moving footage and should change approximately every four seconds to follow the narration.
