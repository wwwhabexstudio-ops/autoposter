{
  "name": "AutoPoster",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {},
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg espeak-ng && pip install -r requirements.txt",
  "forwardPorts": [8501],
  "portsAttributes": {"8501": {"label": "AutoPoster", "onAutoForward": "openBrowser"}}
}
